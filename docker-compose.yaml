services:
  app:
    image: becdetat/partsbin:latest
    ports:
      # Map port 80 (internal) to port 8080 (host)
      - 8080:80
    volumes:
      # Update the first part of this (before the ':') to somewhere safe
      - ./data:/data
volumes:
  data:


